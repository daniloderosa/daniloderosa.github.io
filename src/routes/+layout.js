// Tutte le pagine vengono generate come HTML statico al momento della build.
export const prerender = true;

// Gli indirizzi finiscono sempre con la barra ( /it/ , non /it ).
// Serve a GitHub Pages, che con la barra trova sempre il file giusto.
export const trailingSlash = 'always';
